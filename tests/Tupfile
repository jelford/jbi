include_rules
include Testrules.tup

CFLAGS += -lboost_unit_test_framework


: test_main.cc | <libs> <main> <tests> |> g++ $(CFLAGS) $(TEST_LIBS) -o %o test_main.cc **/test_*.so |> run_tests

.gitignore
